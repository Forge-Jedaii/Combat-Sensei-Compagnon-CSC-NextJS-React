import { createClient } from "@/lib/supabase/server"; import { jsonData, withApiHandler } from "@/lib/api/responses"; import { objectBody, requiredString } from "@/lib/api/validation"; import { ApiError } from "@/lib/api/errors";
export async function POST(request: Request) { return withApiHandler(async () => { const body = objectBody(await request.json()); const email = requiredString(body, "email", 320).toLowerCase(); const password = requiredString(body, "password", 200); const supabase = await createClient(); const { data, error } = await supabase.auth.signInWithPassword({ email, password }); if (error) throw new ApiError("Identifiants invalides.", 401, "INVALID_CREDENTIALS"); return jsonData({ user: { id: data.user.id, email: data.user.email, emailVerified: Boolean(data.user.email_confirmed_at) } }); }); }
