import { createClient } from "@/lib/supabase/server"; import { pagination } from "@/lib/api/pagination"; import { jsonData, withApiHandler } from "@/lib/api/responses"; import { ApiError } from "@/lib/api/errors"; import { ProfileService } from "@/services";
export async function GET(request: Request) { return withApiHandler(async () => { const p = pagination(request.url); const result = await new ProfileService(await createClient()).list(p.from, p.to); return jsonData({ ...result, page: p.page, pageSize: p.pageSize }); }); }
export async function POST() { return withApiHandler(async () => { throw new ApiError("Les profils sont créés automatiquement lors de l’inscription.", 405, "METHOD_NOT_ALLOWED"); }); }
